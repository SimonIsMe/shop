<h1><%= @category.name %></h1>
<a href="/"> <- Wróć</a>

<p>
  Lista dostępnych produktów:
<ul>
  <% @products.each do |product| %>
      <li>
        <%= product.name %>
        - <a href="/products/<%= product.id %>">Zobacz</a>
      </li>
  <% end %>
</ul>
</p>